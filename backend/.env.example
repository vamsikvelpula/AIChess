STOCKFISH_PATH=/opt/homebrew/bin/stockfish
SECRET_KEY=change-me-to-a-random-secret
# DATABASE_URL left unset - app/config.py builds an absolute path to backend/data/chess_app.db
# (Flask-SQLAlchemy resolves relative sqlite paths against the instance folder, not the cwd)
CORS_ORIGIN=http://localhost:5173
